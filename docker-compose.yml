version: '3.8'

services:
  analyzer:
    build: .
    image: whatsapp-protocol-analyzer:latest
    env_file:
      - .env.example
    volumes:
      - ./data:/app/data
      - ./reports:/app/reports
    network_mode: none # Siber güvenlik izolasyonu için dış ağ erişimi kapatıldı